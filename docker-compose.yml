services:
  getafix:
    image: incredibad/getafix:latest
    container_name: getafix
    ports:
      - "4220:4220"
    volumes:
      - footrack_data:/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  footrack_data:
    external: true
